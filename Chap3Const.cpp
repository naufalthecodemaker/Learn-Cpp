#include <iostream>

int main(){
  /*
    The const keyword specifies that a variable's value is constant 
    and tells the compiler to prevent anything from modifying it
    (read only)
  */

  //example
  const double PI = 3.14159; 
  const int LIGHT_SPEED = 2997992458;
  return 0;
}